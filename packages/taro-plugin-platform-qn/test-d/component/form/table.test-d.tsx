/// <reference path="../../setup.d.ts" />

import React from "react";
import { Table, TableColumn } from "@tarojs/components";

<Table
  dataSource={[]}
  hasBorder
  hasHeader
  isZebra
  loading
  fixedHeader
  maxBodyHeight={100}
  stickyHeader
  offsetTop={10}
  onChange={() => {}}
  onSelect={() => {}}
  onSelectAll={() => {}}
  selectedRowKeys={[]}
  mode="multiple"
  onResizeChange={() => {}}
  onSort={() => {}}
  onFilter={() => {}}
  onBodyScroll={() => {}}
>
  <TableColumn dataIndex="" title="Id" width="" align="left" alignHeader="left" lock resizable />
</Table>;
