{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "inputs": ["src/**"]
    },
    "dev": {
      "cache": false
    },
    "test": {
      "outputs": []
    }
  },
  "globalDependencies": ["tsconfig.base.json"]
}
